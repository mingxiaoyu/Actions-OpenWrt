--- a/drivers/phy/rockchip/Makefile
+++ b/drivers/phy/rockchip/Makefile
@@ -6,6 +6,7 @@ obj-$(CONFIG_PHY_ROCKCHIP_INNO_CSIDPHY)
 obj-$(CONFIG_PHY_ROCKCHIP_INNO_DSIDPHY)	+= phy-rockchip-inno-dsidphy.o
 obj-$(CONFIG_PHY_ROCKCHIP_INNO_HDMI)	+= phy-rockchip-inno-hdmi.o
 obj-$(CONFIG_PHY_ROCKCHIP_INNO_USB2)	+= phy-rockchip-inno-usb2.o
+obj-$(CONFIG_PHY_ROCKCHIP_INNO_USB3)	+= phy-rockchip-inno-usb3.o
 obj-$(CONFIG_PHY_ROCKCHIP_NANENG_COMBO_PHY)	+= phy-rockchip-naneng-combphy.o
 obj-$(CONFIG_PHY_ROCKCHIP_PCIE)		+= phy-rockchip-pcie.o
 obj-$(CONFIG_PHY_ROCKCHIP_SNPS_PCIE3)	+= phy-rockchip-snps-pcie3.o
--- a/drivers/phy/rockchip/Kconfig
+++ b/drivers/phy/rockchip/Kconfig
@@ -48,6 +48,15 @@ config PHY_ROCKCHIP_INNO_USB2
 	help
 	  Support for Rockchip USB2.0 PHY with Innosilicon IP block.
 
+config PHY_ROCKCHIP_INNO_USB3
+	tristate "Rockchip INNO USB 3.0 PHY Driver"
+	default y if ARCH_ROCKCHIP
+	depends on OF
+	select GENERIC_PHY
+	select USB_PHY
+	help
+	  Support for Rockchip USB 3.0 PHY with Innosilicon IP block.
+
 config PHY_ROCKCHIP_INNO_CSIDPHY
 	tristate "Rockchip Innosilicon MIPI CSI PHY driver"
 	depends on (ARCH_ROCKCHIP || COMPILE_TEST) && OF
--- a/arch/arm64/boot/dts/rockchip/rk3328-orangepi-r1-plus-lts.dts
+++ b/arch/arm64/boot/dts/rockchip/rk3328-orangepi-r1-plus-lts.dts
@@ -12,6 +12,40 @@
 / {
 	model = "Xunlong Orange Pi R1 Plus LTS";
 	compatible = "xunlong,orangepi-r1-plus-lts", "rockchip,rk3328";
+	
+	&usb3phy_grf: syscon@ff77c000 {
+		compatible = "rockchip,rk3328-usb3phy-grf", "syscon", "simple-mfd";
+		reg = <0x0 0xff77c000 0x0 0x1000>;
+		#address-cells = <1>;
+		#size-cells = <1>;
+	};
+	
+	&u3phy: usb3-phy@ff470000 {
+		compatible = "rockchip,rk3328-usb3-phy";
+		reg = <0x0 0xff470000 0x0 0x10000>;
+		clocks = <&cru SCLK_USB3PHY_REF>;
+		clock-names = "ref";
+		resets = <&cru SRST_USB3PHY>;
+		reset-names = "phy";
+		rockchip,grf = <&usb3phy_grf>;
+		status = "okay";
+	};
+	
+	&usbdrd3: usb@ff600000 {
+		compatible = "snps,dwc3";
+		reg = <0x0 0xff600000 0x0 0x100000>;
+		interrupts = <GIC_SPI 104 IRQ_TYPE_LEVEL_HIGH 0>;
+		interrupt-names = "dwc3";
+		clocks = <&cru SCLK_USB3OTG_REF>;
+		clock-names = "ref";
+		phys = <&u3phy>;
+		phy-names = "usb3-phy";
+		dr_mode = "host";
+		maximum-speed = "super-speed";
+		snps,quirk-frame-length-adjustment;
+		snps,dis-u2-freeclk-exists-quirk;
+		status = "okay";
+	};
 };
 
 &dmc_opp_table {
@@ -19,6 +53,7 @@
 	/delete-node/ opp-840000000;
 	/delete-node/ opp-924000000;
 	/delete-node/ opp-1056000000;
+	/delete-node/ opp-1608000000;
 };
 
 &gmac2io {
